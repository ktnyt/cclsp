// Re-export all adapter-related types from the canonical location
// This file is kept for backward compatibility with existing imports
export type {
  InitializeParams,
  ServerAdapter,
  ServerState,
} from '../types.js';
